'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCharset = getCharset;
var numbers = exports.numbers = '0123456789';
var charsLower = exports.charsLower = 'abcdefghijklmnopqrstuvwxyz';
var charsUpper = exports.charsUpper = charsLower.toUpperCase();
var hexChars = exports.hexChars = 'abcdef';

function getCharset(type) {
  if (type === 'alphanumeric') return numbers + charsLower + charsUpper;
  if (type === 'numeric') return numbers;
  if (type === 'alphabetic') return charsLower + charsUpper;
  if (type === 'hex') return numbers + hexChars;

  // Customized charset
  var chars = {};
  var len = type.length;
  for (var i = 0; i < len; ++i) {
    chars[type[i]] = true;
  }return Object.keys(chars).sort().join('');
}