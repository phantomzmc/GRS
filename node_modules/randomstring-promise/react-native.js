'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var _Buffer = typeof Buffer !== 'undefined' ? Buffer : require('buffer').Buffer;
var RNRandomBytes = require('react-native').NativeModules.RNRandomBytes;
var randomBytes = function randomBytes(length, cb) {
  RNRandomBytes.randomBytes(length, function (err, base64String) {
    if (err) {
      cb(err);
    } else {
      cb(null, new _Buffer(base64String, 'base64'));
    }
  });
};

exports.default = require('./gen')(randomBytes);